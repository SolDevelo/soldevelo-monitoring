# Secrets / environment
.env

# Rendered configs (output of bin/render-configs.sh from *.template files)
/prometheus/prometheus.yml
/prometheus/prometheus-meta.yml
/alertmanager/alertmanager.yml
/caddy/Caddyfile

# Per-deployment blackbox probe URLs (committed per project as part of the
# overlay, not in the base package).
/prometheus/targets/blackbox/*.json

# Local editor / OS junk
.DS_Store
.idea/
.vscode/
